# Analysis

- `analysis-plan.md` specifies the estimands, inferential units, tests, and interpretation boundaries.
- `generate_figures.py` validates the principal reported statistics and regenerates all 33 English PNG figures.

Run the script from the repository root after placing authorized inputs in `data/restricted/`:

```bash
.venv/Scripts/python analysis/generate_figures.py
```
