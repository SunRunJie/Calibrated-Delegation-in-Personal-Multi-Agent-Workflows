# Aggregate Results

`tables/` contains the non-disclosive aggregate outputs used to verify the reported results. Participant-level records, link codes, and dated event histories are not included.

The JSON summary records the principal statistical checks; the CSV files provide machine-readable aggregate tables.
